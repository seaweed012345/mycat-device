services:
  frontend:
    image: node:20
    volumes:
      - C:\docker-test\mycat-device\mycat-frontend:/app
    working_dir: /app
    command: bash -c "npm install && npm run dev -- --host 0.0.0.0 --port 5050"
    ports:
      - "5050:5050"
    tty: true